data/seed-*.json
node_modules/
.env*
.next/
*.tsbuildinfo

# Design canvas seeded output (regenerated from the .dc.html working files)
tlv-minyanim-homepage.html

# Generated artboard preview (rebuild with scripts/render-artboards.py)
design/_preview.html

# Standalone comparison pages (regenerable)
design/mobile-homepages.html

# Blur comparison scratch page
design/_blur-compare.html

# Header comparison scratch page
design/_headers.html

# macOS
.DS_Store

# Throwaway probes. They live inside the repo rather than in /tmp because
# `@hebcal/core` and the `@/` alias only resolve from here, and three of them
# reached a commit before this line existed.
.scratch/
